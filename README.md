# File-System-Simulation

A block-based file sysstem simulator in C that models how an operating system manages persistent storage. The system implements a logical disk abstraction, file metadata structures, and free-space management, and exposes APIs for creating, reading, writing, and deleting files.
